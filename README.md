# Example PHP App with Docker

Basic setup using Docker to provision a PHP application, using PHP7, nginx and MySQL.

## Getting started

Spin up containers

    $ docker-compose up -d

Get your docker machine IP

    $ docker-machine ip default

Visit <http://docker-machine-ip:8080>